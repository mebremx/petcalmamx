<section class="section">
  <div class="container" style="text-align:center; max-width:560px;">
    <h1 style="color: var(--color-primary-dark);">Ups, esta página no existe 🐾</h1>
    <p style="color: var(--color-text-muted); margin-bottom:1.5rem;">Parece que tu mascota se llevó este enlace. Vuelve al inicio para encontrar lo que buscas.</p>
    <a href="{{ routes.root_url }}" class="btn btn--primary btn--lg">Volver al inicio</a>
  </div>
</section>

{% schema %}
{
  "name": "404",
  "settings": []
}
{% endschema %}
